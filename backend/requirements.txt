fastapi
uvicorn[standard]
pydantic>=2.9
pydantic-settings
python-multipart
httpx
pypdf
pdfplumber
pypdfium2
python-docx

# Phase 8 — local intelligence (no AI API key required)
sentence-transformers>=3.0    # semantic field<->fact matching, runs on CPU
rapidfuzz>=3.0                # fast fuzzy matching for labels and options
spacy>=3.7                    # NER over prose sources; needs en_core_web_sm
python-dateutil>=2.9          # date normalisation
phonenumbers>=8.13            # phone parsing (only for +CC numbers)
nameparser>=1.1               # split full names into parts
usaddress>=0.5.10             # split US addresses into components

# Auth + MongoDB
motor>=3.6                      # async MongoDB driver
bcrypt>=4.0                     # password hashing
python-jose[cryptography]>=3.3  # JWT encode/decode
email-validator>=2.0            # email validation for pydantic EmailStr

# Tests
pytest
